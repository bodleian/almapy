[project]
name = "almapy"
version = "5.20.1"
requires-python = ">=3.8.1, <4.0.0"
authors = [{ name = "Ben Gable", email = "ben.gable@bodleian.ox.ac.uk" }]
dependencies = [

  "httpx>=0.27.0",
  "xmltodict>=0.13.0",
  "python-box>=7.2.0",
  "glom>=23.5.0",
  "gracy>=1.32.1",
  "typing-extensions (>=4.12.2,<5.0.0)",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
publish-url = "https://gitlab.bodleian.ox.ac.uk/api/v4/projects/779/packages/pypi"

[dependencies-groups]
dev = [
  "commitizen>=3.29.0",
  "coverage>=7.6.1",
  "mypy>=1.11.1",
  "pre-commit>=3.5.0",
  "pytest>=8.3.2",
  "pytest-asyncio>=0.23.8",
  "pytest-clarity>=1.0.1",
  "pytest-mock>=3.14.0",
  "ruff>=0.6.1",
  "safety>=2.3.5",
  "typeguard>=4.3.0",
  "environs>=11.0.0",
]

[tool.commitizen]
name = "cz_conventional_commits"
version = "5.20.1"
tag_format = "$version"
version_files = ["pyproject.toml:^version"]

[tool.coverage.report] # https://coverage.readthedocs.io/en/latest/config.html#report
fail_under = 50
precision = 1
show_missing = true
skip_covered = true

[tool.coverage.run] # https://coverage.readthedocs.io/en/latest/config.html#run
branch = true
command_line = "--module pytest"
data_file = "reports/.coverage"
source = ["src"]

[tool.coverage.xml] # https://coverage.readthedocs.io/en/latest/config.html#xml
output = "reports/coverage.xml"

[tool.mypy] # https://mypy.readthedocs.io/en/latest/config_file.html
junit_xml = "reports/mypy.xml"
strict = true
disallow_subclassing_any = false
disallow_untyped_decorators = false
ignore_missing_imports = true
pretty = true
show_column_numbers = true
show_error_codes = true
show_error_context = true
warn_unreachable = true

[tool.pytest.ini_options] # https://docs.pytest.org/en/latest/reference/reference.html#ini-options-ref
addopts = "--import-mode=importlib --color=yes --doctest-modules --exitfirst --failed-first --strict-config --strict-markers --typeguard-packages=almapy --verbosity=2 --junitxml=../reports/pytest.xml"
filterwarnings = ["error", "ignore::DeprecationWarning"]
testpaths = ["src", "tests"]
xfail_strict = true


[tool.ruff] # https://github.com/charliermarsh/ruff
fix = true

line-length = 100
preview = true
src = ["dire", "tests"]
target-version = "py38"


[tool.ruff.lint]
select = [
  # flake8-2020
  "YTT",
  # flake8-bandit
  "S",
  # flake8-bugbear
  "B",
  # flake8-builtins
  "A",
  # flake8-comprehensions
  "C4",
  # flake8-debugger
  "T10",
  # flake8-simplify
  "SIM",
  # isort
  "I",
  # mccabe
  "C90",
  # pycodestyle
  "E",
  "W",
  # pyflakes
  "F",
  # pygrep-hooks
  "PGH",
  # pyupgrade
  "UP",
  # ruff
  "RUF",
  # tryceratops
  "TRY",
]


ignore = ["E501", "S307", "RET504", "S101", "PERF203", "D107", "ISC001"]

unfixable = ["ERA001", "F401", "F841", "T201", "T203"]
